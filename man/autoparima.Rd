% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autouparima.R
\name{autoparima}
\alias{autoparima}
\title{Parallel ARIMA Model Selection (forecast backend)}
\usage{
autoparima(
  data,
  max_p,
  max_q,
  max_lag,
  workers = 4,
  criterion = c("AIC", "BIC", "AICc"),
  include_mean = TRUE
)
}
\arguments{
\item{data}{Numeric, ts, or zoo series.}

\item{max_p}{Maximum AR order.}

\item{max_q}{Maximum MA order.}

\item{max_lag}{Lag length for Ljung-Box test.}

\item{workers}{Number of parallel workers.}

\item{criterion}{Selection criterion: "AIC", "BIC", or "AICc".}

\item{include_mean}{Logical. Include mean in ARIMA model.}
}
\value{
A fitted \code{forecast::Arima} model.
}
\description{
Performs parallel grid search over ARIMA(p,d,q) models using
\code{forecast::Arima} and selects the best model according to
AIC, BIC, or AICc after Ljung-Box residual diagnostic filtering.
}
\examples{
\dontrun{
model <- autoparima(
  data = rnorm(500),
  max_p = 4,
  max_q = 4,
  max_lag = 20,
  workers = 2,
  criterion = "AIC"
)
summary(model)
}
}
